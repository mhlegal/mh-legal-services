# Set this only when deploying the frontend separately from the backend.
# Example for Vercel deployment with backend on Replit:
#   VITE_API_URL=https://mh-legal-services.replit.app
#
# Leave UNSET for same-origin Replit deployments — the frontend will use
# the local /api path served by the same host.
VITE_API_URL=
